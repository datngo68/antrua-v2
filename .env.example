# Copy to .env.local (gitignored)

# Absolute or relative path to SQLite (legacy QLTD DB or local seed)
ANTRUA_SQLITE_PATH=./data/antrua.dev.db

# iron-session password — min 32 characters
SESSION_PASSWORD=complex_password_at_least_32_characters_long
